# Vidra Extensions Ledger

Vidra-specific features are tracked separately from PeerTube parity so Ralph does not confuse extras with completion of parity.

Status values: `NOT_STARTED`, `FRONTEND_PARTIAL`, `VERIFIED`, `INTENTIONAL_DIFFERENCE`, `DEFERRED`, `BLOCKED`.

> **Reconciliation (2026-07-03 close-out).** Rows below reflect the frontend reality after a
> green `npm run ci` and the **74 passed / 0 failed** backed run. MSG-NORMAL, MSG-E2EE (real
> Olm), and DONATION-WALLET are now `VERIFIED` — their `e2e-backed/*` specs are part of the
> standard backed run. ATProto stays `FRONTEND_PARTIAL` (live-PDS-gated). Whisper is
> `FRONTEND_PARTIAL` (UI shipped + mocked; backed spec `WHISPER_ENABLED`-gated). IPFS-STORAGE
> and CLAMAV are `DEFERRED` with reasons — no in-scope frontend item is left unclassified.

| id | feature | scope | backend requirements | frontend requirements | tests required | status | evidence | notes |
|---|---|---|---|---|---|---|---|---|
| VIDRA-ATPROTO | ATProto/Bluesky integration (v1: outbound cross-posting) | federation | Modular config (ATPROTO_ENABLED); enable with ActivityPub, alone, or disabled; SSRF-safe PDS client; app password sealed at rest, never returned/logged; auto-announce new PUBLIC videos | User Bluesky connection settings (link form w/ APP-password guidance + help link, auto-post toggle, linked status handle/DID/last-post, unlink, honest 422/502/503 states); ATProto ProtocolBadge variant | unit (endpoints), Playwright (mocked), backed (written, live-PDS-gated), docs | FRONTEND_PARTIAL | Slice `user-atproto-ui` (2026-07-03): consumes committed `PUT\|GET\|DELETE /api/v1/me/atproto` (ran `npm run codegen`). `/settings/connections` → `components/ConnectionsView.tsx` (Bluesky section, `<ProtocolBadge protocol="atproto">`, connect form w/ APP-password guidance + `bsky.app` help link, auto-post `Toggle`, optional custom PDS, linked status handle/DID/PDS/last-post, re-auth auto-post toggle, armed Unlink, 422/502/503 honest states); `SettingsView` "Connected accounts" link; `ProtocolBadge` gained the `atproto` variant. `api.{get,link,unlink}ATProtoAccount` + `ATProto{LinkRequest,Status}` types (5 endpoint unit tests, 168 green). Mocked `e2e/connections.spec.ts` (5). Backed `e2e-backed/atproto.spec.ts` **WRITTEN, not run** (E2E_ATPROTO-gated, live PDS). `npm run ci` green. | v1 is OUTBOUND announce-only (no ATProto INGEST → remote surfaces stay ActivityPub); auto-post toggle re-collects the app password on change (PUT-only contract re-verifies creds — INTENTIONAL_DIFFERENCE, documented); VERIFIED pending a live-PDS backed run; must not break ActivityPub parity |
| VIDRA-IPFS-STORAGE | IPFS storage/distribution | storage/media | IPFS adapter/gateway/pinning, content metadata, fallback modes | IPFS status/config UI where supported | integration, smoke, Playwright | DEFERRED | No IPFS UI shipped; the media architecture serves from filesystem/S3 + HLS today. The IPFS storage/distribution adapter + its status/config UI are deferred until the backend adapter lands | Use uploaded Vidra IPFS UX docs when present |
| VIDRA-MSG-NORMAL | Normal secure messaging | messaging | server-side message body, attachments, SSRF-safe previews, disappearing timers | inbox/thread/composer/attachments/link previews/timer UI | integration, Playwright, security | VERIFIED | e2e-backed/messaging.spec.ts (send + attachment + read-receipt persist cross-user) + report.spec.ts (DM report), both part of the 74/0 backed run; inbox/thread/composer/attachments/link-previews/read-receipts shipped (disappearing timers are an E2EE feature) | Separate from PeerTube parity |
| VIDRA-MSG-E2EE | Encrypted messaging | messaging/security | ciphertext-only storage, key/device metadata, test vectors, no homebrew crypto | encryption state UI, composer/thread, no plaintext logging | test vectors, integration, Playwright, security | VERIFIED | Slice `user-e2ee-ui` (2026-07-03): client-side Olm E2EE UX per e2ee.md §5 over committed `/api/v1/e2ee/*`. `lib/e2ee/*` (envelope fan-out/timer/safety-number [pure, unit-tested], Olm loader/provider, IndexedDB pickle store, engine); `components/e2ee/*` (DeviceSetup, EncryptedThreadView with lock/undecryptable/timer/safety-numbers, StartEncryptedButton contract-gated, DevicesView); `/settings/devices`; inbox/thread lock indicators. Mocked `e2e/e2ee.spec.ts` (7, stubbed crypto seam) + unit (envelope 9, engine 3, endpoints +11). Real-crypto backed `e2e-backed/e2ee.spec.ts` is EXECUTED + PASSING in the 74/0 backed run (real Olm, two contexts, ciphertext-only server, short-TTL vanish). `npm run ci` green (mocked path). | Threat model resolved in e2ee.md; VERIFIED via the backed run — the real Olm path runs in `e2e:backed`, not the mocked `npm run ci` |
| VIDRA-WHISPER-CAPTIONS | Whisper auto captions | media/captions | optional worker/config, job status, caption review/edit | caption generation controls/status/edit flow | integration, smoke, Playwright | FRONTEND_PARTIAL | Auto-caption request UI shipped in the studio CaptionsManager (request → poll → track, plus 503-disabled / 409-follow / 422 states) — e2e/studio.spec.ts (mocked); e2e-backed/whisper-captions.spec.ts WRITTEN but WHISPER_ENABLED-gated (E2E_WHISPER), NOT in the 74/0 run | Optional by config |
| VIDRA-CLAMAV | Virus scanning | security/media | ClamAV integration, fail-open/closed policy, scan states | upload status/error messaging | unit, integration, smoke | DEFERRED | Backend ClamAV fallback modes shipped; the frontend surfaces the moderation-hold quarantine states (P6.2), but a DISTINCT ClamAV scan-pending / infected-file upload state is deferred until the backend exposes a scan-state signal on the video/upload contract | Production should fail closed unless spec says otherwise |
| VIDRA-DONATION-WALLET | Simple verified crypto wallet display | profile/settings | address storage, challenge verification, no custody/no payment processing | profile wallet display, settings verification | unit, integration, Playwright | VERIFIED | Slice `user-donations-ui` (2026-07-03): NON-CUSTODIAL display+copy over committed `/api/v1/**/donation-addresses`. `lib/donation-address.ts` (curated set + per-network validator + verify-support metadata; 11 unit tests); `api.{listMy,add,delete,challenge,verify}DonationAddress` + public `listUser/ChannelDonationAddresses` (7 endpoint tests). `/settings/donations` (`DonationSettingsView`: add w/ network select + per-network validation + optional label + channel-scope select, list, delete, ethereum challenge/verify flow, honest "not available" for btc/ltc/xmr); public `DonateButton` dialog on the channel page (channel + owner reads, copy + verified badge + honesty line, hidden when empty); `DonationBadge`. Mocked `e2e/donations.spec.ts` (8). Backed `e2e-backed/donations.spec.ts` is EXECUTED + PASSING in the 74/0 backed run (add/delete persistence + public channel/user reads). `npm run ci` green (mocked path). | VERIFIED via the backed run (donations.spec.ts is part of the 74/0 run; `npm run ci` stays mocked); no PATCH/edit in contract (add/delete shape); no standalone public user-profile route yet (channel page is the identity surface); premium/payments remain deferred |
